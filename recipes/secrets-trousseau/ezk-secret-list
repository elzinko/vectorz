#!/usr/bin/env bash
# DÉPRÉCIÉ — fine redirection vers « ezk-secret list », le temps de la transition.
DIR="$(cd "$(dirname "$0")" && pwd)"
printf '⚠ « ezk-secret-list » est déprécié — utilise « ezk-secret list ». Redirection…\n' >&2
exec "$DIR/ezk-secret" list "$@"
