#!/usr/bin/env bash
# DÉPRÉCIÉ — fine redirection vers « ezk-secret check », le temps de la transition.
DIR="$(cd "$(dirname "$0")" && pwd)"
printf '⚠ « ezk-secret-check » est déprécié — utilise « ezk-secret check ». Redirection…\n' >&2
exec "$DIR/ezk-secret" check "$@"
