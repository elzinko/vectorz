#!/usr/bin/env bash
# DÉPRÉCIÉ — fine redirection vers « ezk-secret get », le temps de la transition.
DIR="$(cd "$(dirname "$0")" && pwd)"
printf '⚠ « ezk-secret-get » est déprécié — utilise « ezk-secret get ». Redirection…\n' >&2
exec "$DIR/ezk-secret" get "$@"
