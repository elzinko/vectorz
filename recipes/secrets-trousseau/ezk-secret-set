#!/usr/bin/env bash
# DÉPRÉCIÉ — fine redirection vers « ezk-secret set », le temps de la transition.
# À retirer une fois les appelants migrés (README, recettes lancement-app).
DIR="$(cd "$(dirname "$0")" && pwd)"
printf '⚠ « ezk-secret-set » est déprécié — utilise « ezk-secret set ». Redirection…\n' >&2
exec "$DIR/ezk-secret" set "$@"
