# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE & AI tools (machine-specific)
.vscode/
.idea/
.claude/
.codex/
.cursor/
.gemini/
*.swp
*.swo

# cop1 runtime data (execution artifacts, not source code)
.cop1/

# BMAD framework (reinstallable via bmad install, version in _bmad/_config/manifest.yaml)
_bmad/core/
_bmad/bmm/
_bmad/bmb/
_bmad/tea/
# Keep _bmad/_config/ and _bmad/_memory/ tracked (project-specific customizations)

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Database
*.db
*.sqlite
*.sqlite3

# Testing
coverage/
.playwright-mcp/

# Dogfood / cobaye verification screenshots (throwaway)
cobaye-*.png

# Temp
tmp/
temp/

# ezk-sprint scratch (éphémère par-sprint, jamais commité — survit à la compaction, pas au repo)
SPRINT.md

# Local config (may contain machine-specific paths/models)
cop1.config.yaml
.supervision/
# Branchement de l'émetteur : artefact d'INSTALLATION local, régénéré par
# `pnpm --dir products/mega-city supervision:link .` (ADR-034). Chemins absolus
# machine-spécifiques ; commité, il casserait sur tout autre poste et neutraliserait
# l'échappatoire SUPERVISION_PER_WORKTREE (ADR-019).
# Ancré à la racine : l'ADR ne décide que du fichier de branchement du dépôt,
# pas de tout `.mcp.json` à n'importe quelle profondeur.
/.mcp.json
docs/dogfood-reports/
